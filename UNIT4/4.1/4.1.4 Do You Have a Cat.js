/* This program should declare a boolean that describes whether or
 * not you have a cat. Then you should print out an informative
 * message to the user. */
function start(){
    var doNotHaveACat = false;
    
    print(doNotHaveACat);
}