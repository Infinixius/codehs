function start(){
    var name = readLine("What is your name? ");
    var droids = readInt("How many droids would you like to meet? ");
    var wookies = readInt("How many Wookies would you like to meet? ");
    
    println(`Hi ${name}, you want to meet ${droids} droids and ${wookies} Wookies.`);
}